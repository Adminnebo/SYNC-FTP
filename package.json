{
  "name": "nebo-ftp-image-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Daily sync: FTP images -> Supabase Storage (uploads only new images)",
  "scripts": {
    "discover": "node migrate.js discover",
    "test": "node migrate.js test",
    "run": "node migrate.js run",
    "sync": "node migrate.js sync",
    "sync-drive": "node drive-sync.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "basic-ftp": "^5.0.5",
    "dotenv": "^16.4.5"
  }
}
